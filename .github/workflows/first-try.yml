name: Basic CI

on:
  push:
    branchs: ['main']

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - name: Code checkout
        uses: actions/checkout@v4
      - name: Print
        run: composer install
